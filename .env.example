# Optional: CoinGecko Demo API key (free tier)
# Get one at https://www.coingecko.com/en/api/pricing
# Without a key, the script uses slower rate limiting (works fine, just slower)
COINGECKO_API_KEY=your_demo_api_key_here
